{
  "name": "protovalidate-es-quickstart-finish",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "ci": "npm run generate && npm run build && npm run test",
    "build": "tsc --noEmit",
    "generate": "buf generate",
    "test": "vitest run"
  },
  "license": "Apache-2.0",
  "dependencies": {
    "@bufbuild/protobuf": "^2.5.1",
    "@bufbuild/protovalidate": "^0.6.0"
  },
  "devDependencies": {
    "prettier": "^3.5.3",
    "typescript": "^5.8.3",
    "vitest": "^3.2.0"
  }
}
