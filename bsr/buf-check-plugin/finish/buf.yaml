version: v2
modules:
  - path: proto
lint:
  use:
    - MINIMAL
    - RPC_SUFFIX
plugins:
  - plugin: rpc-suffix
    options:
      forbidden_rpc_suffixes:
        - Method
        - RPC
breaking:
  use:
    - FILE
