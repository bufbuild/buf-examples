version: 2
jobs:
  lint:
    docker:
      - image: bufbuild/buf:1.1.0
    steps:
      - checkout:
          path: ~/circleci
      - run: buf lint
  breaking:
    docker:
      - image: bufbuild/buf:1.1.0
    steps:
      - checkout:
          path: ~/circleci
      - run: buf breaking --against https://github.com/bufbuild/buf-examples.git#branch=main,ref=HEAD~1,subdir=proto

workflows:
  version: 2
  protobuf_check:
    jobs:
      - lint
      - breaking
